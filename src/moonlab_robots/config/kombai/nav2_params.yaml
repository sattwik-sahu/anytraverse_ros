amcl:
  ros__parameters:
    enabled: False # Disabled because we are using VIO/Odom only

bt_navigator:
  ros__parameters:
    global_frame: odom
    robot_base_frame: base_link
    use_sim_time: False
    default_bt_xml_filename: "navigate_to_pose_w_replanning_and_recovery.xml"

controller_server:
  ros__parameters:
    controller_frequency: 30.0
    FollowPath:
      plugin: "nav2_mppi_controller::MPPIController"
      time_steps: 56
      model_dt: 0.05
      batch_size: 2000
      vx_std: 0.5
      vy_std: 0.0
      wz_std: 0.6
      vx_max: 1.0
      vx_min: -0.4
      vy_max: 0.0
      wz_max: 1.9
      ax_max: 5.0
      ax_min: -3.0
      ay_min: -3.0
      ay_max: 3.0
      az_max: 3.14
      iteration_count: 1
      temperature: 0.3
      gamma: 0.015
      motion_model: "DiffDrive"
      visualize: false
      reset_period: 1.0 # (only in Humble)
      regenerate_noises: false
      sgf_order: 2
      TrajectoryVisualizer:
        trajectory_step: 5
        time_step: 3
      TrajectoryValidator:
        plugin: "mppi::DefaultOptimalTrajectoryValidator"
        collision_lookahead_time: 2.0
        consider_footprint: false
      AckermannConstraints:
        min_turning_r: 0.2
      critics:
        - ConstraintCritic
        - CostCritic
        - GoalCritic
        - GoalAngleCritic
        - PathAlignCritic
        - PathFollowCritic
        - PathAngleCritic
        - PreferForwardCritic
        - VelocityDeadbandCritic
      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0
      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 1.4
      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        threshold_to_consider: 0.5
      PreferForwardCritic:
        enabled: true
        cost_power: 1
        cost_weight: 10.0
        threshold_to_consider: 0.5
      # ObstaclesCritic:
      #   enabled: true
      #   cost_power: 1
      #   repulsion_weight: 1.5
      #   critical_weight: 20.0
      #   consider_footprint: false
      #   collision_cost: 10000.0
      #   collision_margin_distance: 0.1
      #   near_goal_distance: 0.5
      #   inflation_radius: 0.55 # (only in Humble)
      #   cost_scaling_factor: 10.0 # (only in Humble)
      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.81
        critical_cost: 300.0
        consider_footprint: true
        collision_cost: 1000000.0
        near_goal_distance: 1.0
        trajectory_point_step: 2
      PathAlignCritic:
        enabled: true
        cost_power: 1
        cost_weight: 14.0
        max_path_occupancy_ratio: 0.05
        trajectory_point_step: 4
        threshold_to_consider: 0.5
        offset_from_furthest: 20
        use_path_orientations: false
      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        offset_from_furthest: 5
        threshold_to_consider: 1.4
      PathAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 2.0
        offset_from_furthest: 4
        threshold_to_consider: 0.5
        max_angle_to_furthest: 1.0
        mode: 0
      VelocityDeadbandCritic:
        enabled: true
        cost_power: 1
        cost_weight: 35.0
        deadband_velocities: [0.15, 0.0, 0.15]
      # TwirlingCritic:
      #   enabled: true
      #   twirling_cost_power: 1
      #   twirling_cost_weight: 10.0

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0

      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true

      width: 4
      height: 4
      resolution: 0.1

      always_send_full_costmap: false

      footprint: "[[0.32, 0.15], [0.32, -0.15], [-0.32, -0.15], [-0.32, 0.15]]"

      plugins: ["voxel_layer", "inflation_layer"]

      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        footprint_clearing_enabled: true

        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 12
        max_obstacle_height: 0.6

        unknown_threshold: 10
        mark_threshold: 3
        combination_method: 1

        publish_voxel_map: False

        observation_sources: pointcloud

        pointcloud:
          topic: /obstacle_points
          data_type: "PointCloud2"

          min_obstacle_height: 0.1
          max_obstacle_height: 0.6

          obstacle_max_range: 4.0
          obstacle_min_range: 0.3

          raytrace_max_range: 4.0
          raytrace_min_range: 0.1

          clearing: True
          marking: True

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        inflation_radius: 0.4
        cost_scaling_factor: 4.0

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: odom
      robot_base_frame: base_link
      resolution: 0.1
      footprint: "[ [0.325, 0.15], [0.325, -0.15], [-0.325, -0.15], [-0.325, 0.15] ]"
      rolling_window: true
      width: 100
      height: 100
      plugins:
        - obstacle_layer
        - inflation_layer
        - denoise_layer

      # voxel_layer:
      #   plugin: "nav2_costmap_2d::VoxelLayer"
      #   enabled: True
      #   footprint_clearing_enabled: true
      #   publish_voxel_map: False
      #   origin_z: 0.0
      #   z_resolution: 0.05
      #   z_voxels: 12
      #   max_obstacle_height: 0.6
      #   unknown_threshold: 10
      #   mark_threshold: 0
      #   observation_sources: pointcloud
      #   combination_method: 1
      #   pointcloud: # no frame set, uses frame from message
      #     topic: /obstacle_points
      #     max_obstacle_height: 0.6
      #     min_obstacle_height: 0.0
      #     obstacle_max_range: 3.0
      #     obstacle_min_range: 0.1
      #     raytrace_max_range: 4.0
      #     raytrace_min_range: 0.1
      #     clearing: True
      #     marking: True
      #     data_type: "PointCloud2"
      #     transport_type: "raw"

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: pointcloud
        pointcloud:
          topic: /obstacle_points
          max_obstacle_height: 1.0
          min_obstacle_height: 0.15
          obstacle_range: 5.0
          raytrace_range: 5.0
          clearing: True # Since we have no static map, clearing is vital to remove old obstacles
          marking: True
          data_type: "PointCloud2"

      denoise_layer:
        plugin: "nav2_costmap_2d::DenoiseLayer"
        enabled: true
        minimal_group_size: 4
        group_connectivity_type: 8

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.4

planner_server:
  ros__parameters:
    planner_plugin: ["GridBased"]

    GridBased:
      plugin: "nav2_smac_planner::SmacPlannerHybrid" # Using Hybrid-A* planner
      downsample_costmap: false
      downsampling_factor: 1
      tolerance: 0.25 # 25 cm to goal is good enough
      allow_unknown: true # Can travel in unknown space, since unknown environment
      max_iterations: 1000000
      max_on_approach_iterations: 1000
      max_planning_time: 5.0 # Planning and smoothing should complete in 5.0 sec
      motion_model_for_search: "REEDS_SHEPP" # Motion model for legged robot
      angle_quantization_bins: 64 # Docs say 72 but meh whatever
      analytic_expansion_ratio: 3.5
      analytic_expansion_max_length: 3.0
      analytic_expansion_max_cost: 200.0
      analytic_expansion_max_cost_override: false
      minimum_turning_radius: 0.2 # Can be smaller for the quadruped, as it is holonomic
      reverse_penalty: 2.0 # Penalty to apply if motion reversing
      change_penalty: 0.0
      non-straight_penalty: 1.0 # No wonky-ass paths
      cost_penalty: 2.0
      retrospective_penalty: 0.015
      lookup_table_size: 20.0 # Size of the dubin/reeds-sheep distance window to cache, in meters.
      cache_obstacle_heuristic: false # Cache the obstacle map dynamic programming distance expansion heuristic between subsequent replannings of the same goal location. Dramatically speeds up replanning performance (40x) if costmap is largely static.
      debug_visualizations: false # For Hybrid nodes: Whether to publish expansions on the /expansions topic as an array of poses (the orientation has no meaning) and the path's footprints on the /planned_footprints topic. WARNING: heavy to compute and to display, for debug only as it degrades the performance.
      use_quadratic_cost_penalty: False
      downsample_obstacle_heuristic: True
      allow_primitive_interpolation: False
      coarse_search_resolution: 4 # Number of bins to skip when doing a coarse search for the path. Only used for all_direction goal heading mode.
      goal_heading_mode: "ALL_DIRECTION" # DEFAULT, BIDIRECTIONAL, ALL_DIRECTION* (Just get to the goal, whatever orientation)
      smooth_path: True # If true, does a simple and quick smoothing post-processing to the path

      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true
        refinement_num: 2

recoveries_server:
  ros__parameters:
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    recovery_plugins: ["spin", "backup", "wait"]
    spin:
      plugin: "nav2_recoveries::Spin"
    backup:
      plugin: "nav2_recoveries::BackUp"
    wait:
      plugin: "nav2_recoveries::Wait"

waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

collision_monitor:
  ros__parameters:
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "cmd_vel_smoothed"
    cmd_vel_out_topic: "cmd_vel"
    transform_tolerance: 0.2
    source_timeout: 5.0
    stop_pub_count: 2

    # Define the sensing sources
    observation_sources: ["pointcloud_source"]
    pointcloud_source:
      type: "pointcloud"
      topic: "/obstacle_points" # The launch file will remap this to your actual topic
      min_height: 0.1
      max_height: 2.0
      enabled: True

    # Define the safety zones (Polygons)
    polygons: ["PolygonStop"]
    PolygonStop:
      type: "polygon"
      # Define a 0.6m x 0.6m stop box centered on the robot
      points: "[[0.35, 0.16], [0.35, -0.16], [-0.35, -0.16], [-0.35, 0.16]]"
      action_type: "stop"
      min_points: 10
      visualize: True
      polygon_pub_topic: "polygon_stop"

velocity_smoother:
  ros__parameters:
    smoothing_frequency: 20.0
    scale_velocities: False
    feedback: "CLOSED_LOOP" # Use OPEN_LOOP if you don't have good wheel encoder feedback
    max_velocity: [1.0, 0.3, 0.86] # x, y, theta
    min_velocity: [-0.5, -0.3, -0.86]
    max_accel: [5.0, 5.0, 3.0]
    max_decel: [-5.0, -5.0, -3.0]
    odom_topic: "odom"
    robot_base_frame: "base_link"

# Default settings, we never really dock the robot
docking_server:
  ros__parameters:
    controller_frequency: 50.0
    initial_perception_timeout: 5.0
    wait_charge_timeout: 5.0
    dock_approach_timeout: 30.0
    undock_linear_tolerance: 0.05
    undock_angular_tolerance: 0.1
    max_retries: 3
    base_frame: "base_link"
    fixed_frame: "odom"
    dock_backwards: false
    dock_prestaging_tolerance: 0.5

    # Types of docks
    dock_plugins: ["simple_charging_dock"]
    simple_charging_dock:
      plugin: "opennav_docking::SimpleChargingDock"
      docking_threshold: 0.05
      staging_x_offset: -0.7
      use_external_detection_pose: true
      use_battery_status: false # true
      use_stall_detection: false # true

      external_detection_timeout: 1.0
      external_detection_translation_x: -0.18
      external_detection_translation_y: 0.0
      external_detection_rotation_roll: -1.57
      external_detection_rotation_pitch: -1.57
      external_detection_rotation_yaw: 0.0
      filter_coef: 0.1

    # Dock instances
    # The following example illustrates configuring dock instances.
    # docks: ['home_dock']  # Input your docks here
    # home_dock:
    #   type: 'simple_charging_dock'
    #   frame: map
    #   pose: [0.0, 0.0, 0.0]

    controller:
      k_phi: 3.0
      k_delta: 2.0
      v_linear_min: 0.15
      v_linear_max: 0.15
